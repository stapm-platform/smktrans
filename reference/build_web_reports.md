# Build Web Reports for Pkgdown

Generates RMarkdown wrapper files in vignettes/articles. These wrappers
load the saved configuration and render the report body.

## Usage

``` r
build_web_reports()
```
