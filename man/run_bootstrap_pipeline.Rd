% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_bootstrap_pipeline.R
\name{run_bootstrap_pipeline}
\alias{run_bootstrap_pipeline}
\title{Run the Master Bootstrap Pipeline}
\usage{
run_bootstrap_pipeline(
  config,
  survey_data,
  pops,
  tob_mort_data,
  tob_mort_data_cause,
  B = 100
)
}
\arguments{
\item{config}{A list containing model configuration parameters (e.g., country, years, ages).}

\item{survey_data}{A data.table or data.frame containing the base survey data.}

\item{pops}{A data.table containing population denominators.}

\item{tob_mort_data}{A data.table containing general tobacco mortality data.}

\item{tob_mort_data_cause}{A data.table containing cause-specific tobacco mortality data.}

\item{B}{Integer. The number of bootstrap iterations to run. Defaults to 100.}

\item{seed}{Integer. Master seed for the resampling, taken from \code{config$seed}.
Supplying it makes the run reproducible; NULL restores the old unseeded
behaviour and warns.}
}
\value{
A list containing five massive data.tables with all bootstrap iterations combined:
  \code{init}, \code{quit}, \code{quit_no_init}, \code{relapse}, and \code{net}.
}
\description{
Executes the optimized bootstrapping loop for smoking transition probabilities.
This function pre-calculates mortality risks once to save memory, then iterates
through `B` bootstrap samples, saving intermediate results to a temporary directory
before combining them into final output tables.
}
